<UserControl x:Class="eodh.Views.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <DockPanel>
        <!-- Header -->
        <DockPanel DockPanel.Dock="Top" Margin="8,8,8,4">
            <TextBlock Text="Workspaces" FontWeight="SemiBold" FontSize="14" />
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    HorizontalAlignment="Right"
                    Padding="8,2" FontSize="11" />
        </DockPanel>

        <!-- Workspace selector -->
        <ComboBox DockPanel.Dock="Top"
                  ItemsSource="{Binding Workspaces}"
                  DisplayMemberPath="Name"
                  SelectedItem="{Binding SelectedWorkspace}"
                  Margin="8,4" />

        <!-- Members list -->
        <Expander DockPanel.Dock="Top"
                  Header="Members" Margin="8,4"
                  IsExpanded="False">
            <ItemsControl ItemsSource="{Binding Members}"
                          Margin="8,4">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding Username}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Role, StringFormat=' ({0})'}"
                                       Opacity="0.6" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Expander>

        <!-- Assets list -->
        <TextBlock DockPanel.Dock="Top"
                   Text="Assets" FontWeight="SemiBold"
                   Margin="8,8,8,4" />

        <ListView ItemsSource="{Binding Assets}"
                  Margin="8,0"
                  BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="4,6" BorderThickness="0,0,0,1"
                            BorderBrush="Gray">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontWeight="SemiBold" FontSize="11" />
                                <TextBlock Text="{Binding Status}"
                                           FontSize="10" Opacity="0.6" />
                            </StackPanel>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding PurchasedAt, StringFormat='{}{0:yyyy-MM-dd}'}"
                                       FontSize="10" Opacity="0.5"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</UserControl>
