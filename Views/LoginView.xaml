<UserControl x:Class="eodh.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:eodh.Converters"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="16">
        <StackPanel VerticalAlignment="Center" MaxWidth="350">
            <TextBlock Text="EO Data Hub"
                       FontSize="18" FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />

            <TextBlock Text="Sign in with your EODH credentials to search and access satellite imagery."
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,24"
                       Opacity="0.7" />

            <!-- Environment -->
            <TextBlock Text="Environment" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding Environments}"
                      SelectedItem="{Binding SelectedEnvironment}"
                      Margin="0,0,0,12" />

            <!-- Username -->
            <TextBlock Text="Username" Margin="0,0,0,4" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12" />

            <!-- API Token -->
            <TextBlock Text="API Token" Margin="0,0,0,4" />
            <PasswordBox x:Name="ApiTokenBox"
                         Margin="0,0,0,16" />

            <!-- Error message -->
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"
                       Margin="0,0,0,12" />

            <!-- Login button -->
            <Button Content="Sign In"
                    Command="{Binding LoginCommand}"
                    HorizontalAlignment="Stretch"
                    Padding="8,6"
                    IsDefault="True" />

            <!-- Loading indicator -->
            <ProgressBar IsIndeterminate="True"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                         Margin="0,8,0,0"
                         Height="3" />
        </StackPanel>
    </Grid>
</UserControl>
